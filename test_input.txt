program example;
    func fact (n:int) returns (long)
        begin
            if (n <= 1) then return 1
            else return(n*fact(n-1))
        end
    /* comment line */
    var i:int;
    begin
        for (i=1 to 10)
            begin
                call print(fact(i));
            end
    end .